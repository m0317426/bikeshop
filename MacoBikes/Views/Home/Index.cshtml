
    @model  List<Model.User>;
    @{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_MasterPage.cshtml";
    string image = "/images/bikes/bike" + ViewData["ImageNumber"] + ".jpg";

    }

    <style>

        h1 {
            padding-top: 20px;
            text-align: left;
            text-size-adjust: auto;
        }

        .center {
            position: center;
            top: 50%;
            left: 50%;
            transform: translate(-50%,-50%);
        }

        img {
            width: 600px;
        }
    </style>

    <h1>MacoBikes</h1>

    <div class="text-center">
        <a asp-controller="Product" asp-action="Index"><img src="@image" class="rounded mx-auto d-block" /></a>
    </div>



    @*@foreach (var item in Model)
        {
            <p>@item.Name</p>
        }*@

